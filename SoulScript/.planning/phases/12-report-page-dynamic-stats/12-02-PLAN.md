---
phase: 12-report-page-dynamic-stats
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/hooks/useReport.ts
  - src/app/report/page.tsx
  - __tests__/hooks/useReport.test.tsx
autonomous: true
requirements: [R7, R8]
must_haves:
  truths:
    - "Report page fetches real data from GET /api/report on load"
    - "Report page shows actual entry count, days journaled, and mood distribution from DB"
    - "Report page shows streak data (current and best)"
    - "Report page displays the AI-generated report summary, dominant mood, insights, and recommendations when available"
    - "When fewer than 10 entries exist, partial stats show but AI sections (Pattern Recognition, Emotional Rhythm, Actionable Frameworks) are hidden"
    - "Loading state shows skeleton while data is fetching"
    - "Error state displays a message when fetch fails"
    - "Report defaults to current month but accepts ?month=YYYY-MM URL param"
  artifacts:
    - path: "src/hooks/useReport.ts"
      provides: "TanStack Query hook for GET /api/report"
      exports: ["useReport"]
    - path: "src/app/report/page.tsx"
      provides: "Report page with dynamic data fetching and conditional rendering"
    - path: "__tests__/hooks/useReport.test.tsx"
      provides: "Unit tests for useReport hook"
  key_links:
    - from: "src/app/report/page.tsx"
      to: "src/hooks/useReport.ts"
      via: "import { useReport } from '@/hooks/useReport'"
      pattern: "import.*useReport"
    - from: "src/hooks/useReport.ts"
      to: "/api/report"
      via: "fetch(`/api/report?month=${month}`)"
      pattern: "fetch.*api/report"
    - from: "src/app/report/page.tsx"
      to: "src/lib/mood-themes.ts"
      via: "import { MOOD_EMOJIS } from '@/lib/mood-themes'"
      pattern: "import.*MOOD_EMOJIS"
---

<objective>
Create the TanStack Query hook for fetching report data and refactor the report page to display real dynamic data instead of hardcoded mock values.

Purpose: This completes the end-to-end data flow from DB to UI. After this plan, the report page shows the user's actual emotional patterns, mood distribution percentages, streak stats, and AI-generated insights — all computed from real journal entries.

Output: `src/hooks/useReport.ts` (new), refactored `src/app/report/page.tsx`, and `__tests__/hooks/useReport.test.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-report-page-dynamic-stats/12-CONTEXT.md
@.planning/phases/12-report-page-dynamic-stats/12-RESEARCH.md
@.planning/phases/12-report-page-dynamic-stats/12-PATTERNS.md
@.planning/phases/12-report-page-dynamic-stats/12-01-SUMMARY.md
@src/hooks/useTodayEntries.ts
@src/app/report/page.tsx
@src/lib/mood-themes.ts
@__tests__/hooks/useTodayEntries.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useReport hook and hook tests</name>
  <files>src/hooks/useReport.ts, __tests__/hooks/useReport.test.tsx</files>
  <read_first>
    - src/hooks/useTodayEntries.ts (exact hook pattern to follow: "use client", useQuery shape, queryKey, staleTime, queryFn with fetch + error handling, enabled guard)
    - __tests__/hooks/useTodayEntries.test.tsx (test pattern: @vitest-environment jsdom, createWrapper with QueryClientProvider, vi.stubGlobal("fetch"), renderHook + waitFor)
    - src/lib/report-stats.ts (just created — the types MoodDistributionItem and StreakResult to reference for response shape)
  </read_first>
  <action>
    Create `src/hooks/useReport.ts` following the exact pattern from `src/hooks/useTodayEntries.ts`:

    1. Add `"use client"` directive at top.
    2. Import `useQuery` from `@tanstack/react-query`.
    3. Define `ReportStats` interface: `{ entryCount: number; daysJournaled: number; moodDistribution: MoodDistributionItem[]; streak: StreakResult }` where MoodDistributionItem and StreakResult come from `@/lib/report-stats`.
    4. Define `ReportData` interface: `{ summary: string; dominantMood: string; insights: string; recommendations: string[] }`.
    5. Define `ReportResponse` interface: `{ stats: ReportStats; report: ReportData | null }`.
    6. Export `useReport(month: string)` function returning `useQuery<ReportResponse>` with:
       - `queryKey: ["report", month]`
       - `staleTime: 30_000` (matching useTodayEntries convention)
       - `queryFn`: fetch `/api/report?month=${encodeURIComponent(month)}`, parse JSON, throw on non-ok response with `data.error`
       - `enabled: !!month && /^\d{4}-\d{2}$/.test(month)` (validate format before fetching)

    Create `__tests__/hooks/useReport.test.tsx` following the exact pattern from `__tests__/hooks/useTodayEntries.test.tsx`:
    - `@vitest-environment jsdom` directive
    - `createWrapper()` with QueryClientProvider (retry: false)
    - `vi.stubGlobal("fetch", mockFetch)` in beforeEach
    - Tests:
      a. Calls GET /api/report with correct month param
      b. Returns stats and report data from response
      c. Returns null report when API returns null report field
      d. Throws error on non-ok response
      e. Does not fetch when month is empty string (enabled guard)
      f. Does not fetch when month format is invalid (e.g., "invalid")
  </action>
  <verify>
    <automated>npm run test -- __tests__/hooks/useReport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useReport.ts exports useReport function
    - useReport returns useQuery result with stats and report data
    - useReport has enabled guard that validates month format
    - __tests__/hooks/useReport.test.tsx exists and all tests pass
    - npm run test -- __tests__/hooks/useReport.test.tsx exits 0
  </acceptance_criteria>
  <done>useReport hook fetches from GET /api/report with proper query key, stale time, and enabled guard. All hook tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor report page to display dynamic data with loading/error/empty states</name>
  <files>src/app/report/page.tsx</files>
  <read_first>
    - src/app/report/page.tsx (current 270-line file with all hardcoded data — must understand current structure before modifying)
    - src/hooks/useReport.ts (just created — the hook to import and call)
    - src/lib/mood-themes.ts (MOOD_EMOJIS for emoji lookup on dominant mood)
    - src/app/page.tsx (loading/skeleton pattern to follow for loading state)
    - src/components/NavBar.tsx (NavBar with active="report" prop)
    - .planning/phases/12-report-page-dynamic-stats/12-CONTEXT.md (D-05/D-06 decisions for empty state gating)
  </read_first>
  <action>
    Refactor `src/app/report/page.tsx` to replace all hardcoded data with dynamic fetching. Preserve the existing glassmorphism UI layout, Framer Motion animations, and section structure. Only change the DATA source.

    **Imports to add:**
    - `import { useReport } from "@/hooks/useReport"`
    - `import { useSearchParams } from "next/navigation"`
    - `import { MOOD_EMOJIS } from "@/lib/mood-themes"`

    **Remove hardcoded imports that are no longer needed:** Keep all lucide-react icons that are still used.

    **Month parameter:** Use `useSearchParams()` to get `?month=YYYY-MM` from URL. Default to current month using `new Date().toISOString().slice(0, 7)` (e.g., "2026-07"). This supports future calendar-to-report navigation.

    **Data fetching:** Call `const { data, isLoading, error } = useReport(month)`. Extract `stats` and `report` from `data` when available.

    **Loading state:** When `isLoading` is true, return a skeleton loading state matching the pattern from `src/app/page.tsx` lines 126-136: glass cards with `.skeleton` class for each section (header, big picture, mood distribution).

    **Error state:** When `error` is truthy, show a glass card with error message and a "Back to Calendar" button.

    **Empty state gating (D-05/D-06):** Compute `const hasEnoughEntries = (stats?.entryCount ?? 0) >= 10`. When `hasEnoughEntries` is false OR `report` is null:
    - SHOW: Report header (with real entry count and days journaled from stats)
    - SHOW: Emotional Landscape / mood distribution bar chart (from `stats.moodDistribution`)
    - HIDE: Pattern Recognition section entirely
    - HIDE: Emotional Rhythm section entirely
    - HIDE: Actionable Frameworks section entirely
    - HIDE: "A Moment Worth Noting" section entirely
    - SHOW: Closing reflection (keep as-is, it's generic)
    - SHOW: Action buttons (Back to Calendar)

    **Replace hardcoded header data (line 48-54):**
    - Replace "July 2026" with formatted month from the `month` param (e.g., "July 2026" from "2026-07")
    - Replace "12 entries analyzed" with `${stats.entryCount} entries analyzed`
    - Replace "8 days journaled" with `${stats.daysJournaled} days journaled`

    **Replace hardcoded Big Picture (lines 64-79):**
    - Replace hardcoded "Joy" with `report.dominantMood` (capitalized)
    - Replace hardcoded emoji "😊" with `MOOD_EMOJIS[report.dominantMood] || "💭"`
    - Replace hardcoded description text with `report.summary`

    **Replace hardcoded Emotional Landscape (lines 82-111):**
    - Replace hardcoded distribution array with `stats.moodDistribution.map(item => ...)`
    - Use `item.emotion` for label, `item.emoji` for emoji, `item.percentage` for percentage
    - Keep the bar chart styling (h-1.5 bg-white/5 rounded-full with colored fill)

    **Replace hardcoded Pattern Recognition (lines 113-148):**
    - Only render when `hasEnoughEntries && report`
    - Parse `report.insights` string into individual insight items (split by sentence or newline, take first 3)
    - Render each insight as a card with the existing styling (w-0.5 accent bar, text)

    **Replace hardcoded Emotional Rhythm (lines 150-175):**
    - Only render when `hasEnoughEntries && report`
    - Derive day-of-week patterns from entries: group entries by day of week, find dominant emotion per day. Use a simple statistical approach — iterate over the entries (which are already in the stats response), group by `new Date(e.created_at).getDay()`, and for each day find the most frequent `primary_emotion`. Render each day with its dominant emotion and the emoji from MOOD_EMOJIS.

    **Replace hardcoded "Moment Worth Noting" (lines 177-196):**
    - Only render when `hasEnoughEntries && report`
    - Use `report.summary` (the AI-generated summary_overview from monthly_reports) as the "moment worth noting" text. This is the AI's synthesis of the user's journal entries for the month, which naturally captures the most meaningful moment. Display it in the existing card styling with a heart icon and the user's name in a quote format.

    **Replace hardcoded Actionable Frameworks (lines 199-235):**
    - Only render when `hasEnoughEntries && report`
    - Parse `report.recommendations` array into title/description pairs (the API returns JSON-stringified objects, the MonthlyReport component already has `parseRecommendations` for this)
    - Render each recommendation with the existing card styling

    **Keep unchanged:**
    - NavBar with active="report"
    - Framer Motion containerVariants and itemVariants
    - Glassmorphism card styling
    - Closing reflection section
    - Action buttons (Save Reflection, Back to Calendar)
    - Glow divider
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/report/page.tsx imports and calls useReport hook
    - Report page defaults to current month when no ?month param in URL
    - Report page accepts ?month=YYYY-MM URL parameter
    - Loading state shows skeleton while data is fetching
    - Error state displays error message when fetch fails
    - Header shows real entry count and days journaled from stats
    - Big Picture shows dominant mood emoji and summary from AI report
    - Emotional Landscape shows mood distribution from stats.moodDistribution (not hardcoded percentages)
    - "Moment Worth Noting" section uses report.summary as its text content
    - Pattern Recognition, Emotional Rhythm, Actionable Frameworks sections are hidden when stats.entryCount < 10
    - Pattern Recognition, Emotional Rhythm, Actionable Frameworks sections are hidden when report is null
    - All hardcoded data values ("July 2026", "12 entries", "Joy", pct:45, etc.) are removed
    - npm run build exits 0 (no TypeScript errors)
    - Existing hardcoded Framer Motion variants and glassmorphism styling preserved
  </acceptance_criteria>
  <done>Report page displays real dynamic data from GET /api/report. Loading, error, and empty states handled. AI sections hidden when fewer than 10 entries. "Moment Worth Noting" uses AI report summary. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | useReport hook fetches from GET /api/report with month param |
| API -> Client | Response contains aggregated stats, no raw content |
| Client rendering | Dynamic data rendered in React components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | Report page rendering | accept | Response only contains aggregated stats and AI report text, no PII or encrypted content beyond what user already sees |
| T-12-05 | Tampering | Month URL parameter | mitigate | useReport hook validates month format with regex before enabling query. Invalid months never hit the API. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. All dependencies already present. |
</threat_model>

<verification>
1. `npm run test -- __tests__/hooks/useReport.test.tsx` — all hook tests pass
2. `npm run build` — no TypeScript errors, report page compiles
3. `npm run test` — full test suite passes (no regressions)
4. Manual: navigate to /report, verify loading skeleton appears, then real data renders
5. Manual: navigate to /report?month=2026-07, verify correct month displayed
</verification>

<success_criteria>
- Report page shows real data from DB (not hardcoded mock values)
- Mood distribution percentages match actual entry data
- Streak stats are computed from real consecutive entry days
- AI sections (Pattern Recognition, Actionable Frameworks) only appear when 10+ entries exist
- Loading state provides visual feedback during data fetch
- Error state handles network failures gracefully
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/12-report-page-dynamic-stats/12-02-SUMMARY.md` when done
</output>
