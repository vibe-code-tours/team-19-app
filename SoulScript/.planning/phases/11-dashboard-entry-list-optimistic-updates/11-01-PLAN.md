---
phase: 11-dashboard-entry-list-optimistic-updates
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/utils.ts
  - src/app/api/entries/route.ts
  - src/hooks/useTodayEntries.ts
  - src/hooks/useCreateEntry.ts
  - src/hooks/useDeleteEntry.ts
  - __tests__/lib/utils.test.ts
  - __tests__/hooks/useTodayEntries.test.ts
  - __tests__/hooks/useCreateEntry.test.ts
  - __tests__/hooks/useDeleteEntry.test.ts
autonomous: true
requirements:
  - R7

must_haves:
  truths:
    - "useTodayEntries fetches today's entries from API with day param and returns JournalEntry[]"
    - "useCreateEntry optimistically inserts a temp entry into query cache before server responds"
    - "useDeleteEntry optimistically removes an entry from query cache before server responds"
    - "GET /api/entries accepts optional day (YYYY-MM-DD) param returning single-day entries"
    - "relativeTime returns human-readable relative timestamps from ISO date strings"
    - "JournalEntry interface is importable from @/lib/types and matches API response shape"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "Shared JournalEntry interface"
      exports: ["JournalEntry"]
    - path: "src/lib/utils.ts"
      provides: "relativeTime utility function"
      exports: ["relativeTime"]
    - path: "src/hooks/useTodayEntries.ts"
      provides: "useTodayEntries TanStack Query hook"
      exports: ["useTodayEntries"]
    - path: "src/hooks/useCreateEntry.ts"
      provides: "useCreateEntry optimistic mutation hook"
      exports: ["useCreateEntry"]
    - path: "src/hooks/useDeleteEntry.ts"
      provides: "useDeleteEntry optimistic mutation hook"
      exports: ["useDeleteEntry"]
  key_links:
    - from: "src/hooks/useTodayEntries.ts"
      to: "/api/entries?day=YYYY-MM-DD"
      via: "fetch in queryFn"
      pattern: "fetch.*day="
    - from: "src/hooks/useCreateEntry.ts"
      to: "/api/analyze"
      via: "fetch in mutationFn"
      pattern: "fetch.*analyze"
    - from: "src/hooks/useDeleteEntry.ts"
      to: "/api/entries/[id]"
      via: "fetch in mutationFn"
      pattern: "fetch.*DELETE"
    - from: "src/hooks/useCreateEntry.ts"
      to: "src/lib/types.ts"
      via: "import JournalEntry"
      pattern: "import.*JournalEntry.*from.*@/lib/types"
---

<objective>
Establish the data layer foundation for Phase 11: shared types, utility functions, TanStack Query hooks for fetching/creating/deleting entries, and the API route extension to support single-day queries. Uses TDD — write tests first, then implement to pass.

Purpose: Every downstream component (EntryCard, EntryList, dashboard refactor) depends on these types and hooks. Without this foundation, no UI work can proceed.
Output: 6 new files (types, utils, 3 hooks), 1 modified API route, 4 test files
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-entry-list-optimistic-updates/11-CONTEXT.md
@.planning/phases/11-dashboard-entry-list-optimistic-updates/11-RESEARCH.md
@.planning/phases/11-dashboard-entry-list-optimistic-updates/11-PATTERNS.md
@src/lib/ai/types.ts
@src/components/MoodCalendar.tsx
@src/app/api/entries/route.ts
@src/components/Providers.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared types, utils, and extend GET /api/entries with day param</name>
  <files>src/lib/types.ts, src/lib/utils.ts, src/app/api/entries/route.ts, __tests__/lib/utils.test.ts</files>
  <read_first>
    - src/lib/ai/types.ts (reference for type-only file structure)
    - src/components/MoodCalendar.tsx (lines 8-16: existing JournalEntry interface to extract)
    - src/app/api/entries/route.ts (current GET handler with month-only param)
    - __tests__/lib/mood-themes.test.ts (test structure pattern to follow)
  </read_first>
  <behavior>
    - relativeTime("just now date") returns "just now"
    - relativeTime("5 minutes ago date") returns "5m ago"
    - relativeTime("3 hours ago date") returns "3h ago"
    - relativeTime("2 days ago date") returns "2d ago"
    - relativeTime("30 days ago date") returns formatted month+day string
    - GET /api/entries with ?day=2026-07-15 returns entries created on that day
    - GET /api/entries with ?day=invalid returns 400 error
    - GET /api/entries with ?day=2026-07-15 and no month still works (day param takes precedence)
    - JournalEntry interface is importable and has fields: id, content, primary_emotion, emoji, secondary_emotions, bg_glow_gradient, created_at
  </behavior>
  <action>
    Step 1 (RED): Write `__tests__/lib/utils.test.ts` testing relativeTime with the behavior cases above. Run tests — must fail (utils.ts does not exist).

    Step 2 (GREEN): Create `src/lib/types.ts` extracting JournalEntry interface from MoodCalendar.tsx lines 8-16. Create `src/lib/utils.ts` with `relativeTime(dateString: string): string` function. The function computes diff in ms, converts to min/hr/day, returns "just now" / "Xm ago" / "Xh ago" / "Xd ago" / formatted date for >7 days. Run tests — must pass.

    Step 3 (RED): Extend `__tests__/lib/utils.test.ts` (or create separate entries test) for GET /api/entries with day param. The existing `__tests__/api/entries.test.ts` already covers month param. Add tests for: day param accepted, day param rejected with invalid format, day+month param (day takes precedence). Run tests — must fail (API route does not support day yet).

    Step 4 (GREEN): Modify `src/app/api/entries/route.ts`:
    - Add `const day = searchParams.get("day");` after line 17
    - When `day` is provided and matches `/^\d{4}-\d{2}-\d{2}$/`, compute startDate = `day + "T00:00:00"` and endDate = `day + "T23:59:59.999"`, query with `.gte("created_at", startDate).lte("created_at", endDate)` (note: use `.lte` not `.lt` for end-of-day inclusivity)
    - When `month` is provided, keep existing month-range logic unchanged
    - When neither is valid, return 400: "Provide day (YYYY-MM-DD) or month (YYYY-MM)"
    - Maintain existing auth, decryption, and error handling patterns exactly
    - Run all tests — must pass.

    Per D-13 (query key 'entries', today), D-15 (staleTime 30s), D-16 (invalidate after undo/delete).
  </action>
  <verify>
    <automated>cd /Users/macbookair/Documents/VibeTour/team-19-app/SoulScript && npm run test -- --testPathPattern "utils|entries" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types.ts` exists and exports `JournalEntry` interface with fields: id (string), content (string), primary_emotion (string), emoji (string), secondary_emotions (string[]), bg_glow_gradient (string), created_at (string)
    - `src/lib/utils.ts` exists and exports `relativeTime(dateString: string): string`
    - `relativeTime` returns "just now" for dates <60s ago, "Xm ago" for <60min, "Xh ago" for <24h, "Xd ago" for <7d, formatted date otherwise
    - `src/app/api/entries/route.ts` accepts `day` query param (YYYY-MM-DD format) via regex `/^\d{4}-\d{2}-\d{2}$/`
    - GET /api/entries with `?day=2026-07-15` queries with `gte` at T00:00:00 and `lte` at T23:59:59.999
    - GET /api/entries with invalid day format returns 400
    - GET /api/entries with month param still works (backward compatible)
    - `npm run test` passes with all existing + new tests
  </acceptance_criteria>
  <done>Shared types and utils created, GET /api/entries supports day param, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TanStack Query hooks (useTodayEntries, useCreateEntry, useDeleteEntry)</name>
  <files>src/hooks/useTodayEntries.ts, src/hooks/useCreateEntry.ts, src/hooks/useDeleteEntry.ts, __tests__/hooks/useTodayEntries.test.ts, __tests__/hooks/useCreateEntry.test.ts, __tests__/hooks/useDeleteEntry.test.ts</files>
  <read_first>
    - src/lib/types.ts (created in Task 1 — JournalEntry interface)
    - src/lib/utils.ts (created in Task 1 — relativeTime)
    - src/components/Providers.tsx (QueryClient config: 5min staleTime, 1 retry)
    - src/app/page.tsx (lines 80-120: current handleSubmit and handleUndo patterns to replace)
    - src/app/api/entries/route.ts (after Task 1 modification — day param support)
    - __tests__/api/entries.test.ts (mock pattern: vi.hoisted, vi.mock, chainable)
  </read_first>
  <behavior>
    - useTodayEntries calls GET /api/entries?day=YYYY-MM-DD with today's date
    - useTodayEntries returns JournalEntry[] from data.entries
    - useTodayEntries has staleTime of 30 seconds
    - useTodayEntries queryKey is ['entries', 'today']
    - useCreateEntry calls POST /api/analyze with { content }
    - useCreateEntry onMutate inserts temp entry with id="temp-{uuid}" into ['entries', 'today'] cache
    - useCreateEntry onMutate returns { previous, tempId } context
    - useCreateEntry onError rolls back to previous cache state
    - useCreateEntry onSettled invalidates ['entries', 'today']
    - useDeleteEntry calls DELETE /api/entries/[id]
    - useDeleteEntry onMutate removes entry from ['entries', 'today'] cache
    - useDeleteEntry onError rolls back to previous cache state
    - useDeleteEntry onSettled invalidates ['entries', 'today']
  </behavior>
  <action>
    Step 1 (RED): Create test files:
    - `__tests__/hooks/useTodayEntries.test.ts`: mock fetch to return { entries: [...] }, verify it calls /api/entries?day=today's date, verify returns entries array. Use QueryClientProvider wrapper via renderHook from @testing-library/react.
    - `__tests__/hooks/useCreateEntry.test.ts`: mock fetch for POST /api/analyze, verify onMutate inserts temp entry into cache, verify onSettled invalidates query.
    - `__tests__/hooks/useDeleteEntry.test.ts`: mock fetch for DELETE, verify onMutate removes entry from cache, verify onSettled invalidates query.
    Run tests — must fail (hooks do not exist).

    Step 2 (GREEN): Create the three hooks in `src/hooks/`:

    `useTodayEntries.ts`:
    - "use client" directive
    - Import useQuery from @tanstack/react-query, JournalEntry from @/lib/types
    - Export function returning useQuery with queryKey ['entries', 'today'], staleTime 30_000
    - queryFn: compute today as YYYY-MM-DD, fetch /api/entries?day={today}, return data.entries || []
    - getTodayKey helper: uses Date to compute YYYY-MM-DD string

    `useCreateEntry.ts`:
    - "use client" directive
    - Import useMutation, useQueryClient from @tanstack/react-query, JournalEntry from @/lib/types
    - Export function returning useMutation with mutationFn (POST /api/analyze), onMutate (cancel queries, snapshot previous, insert temp entry with crypto.randomUUID() temp ID, set query data), onError (rollback), onSettled (invalidate)
    - Temp entry defaults: primary_emotion "calm", emoji "😌", secondary_emotions [], bg_glow_gradient "from-sky-500/20 to-blue-600/20" (per Pitfall 5 in RESEARCH: use crypto.randomUUID() not Date.now())

    `useDeleteEntry.ts`:
    - "use client" directive
    - Import useMutation, useQueryClient from @tanstack/react-query, JournalEntry from @/lib/types
    - Export function returning useMutation with mutationFn (DELETE /api/entries/[id]), onMutate (cancel, snapshot, filter out entry), onError (rollback), onSettled (invalidate)

    Run all tests — must pass.
  </action>
  <verify>
    <automated>cd /Users/macbookair/Documents/VibeTour/team-19-app/SoulScript && npm run test -- --testPathPattern "hooks" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useTodayEntries.ts` exists, exports `useTodayEntries()`, uses queryKey `["entries", "today"]`, staleTime is 30000
    - `src/hooks/useCreateEntry.ts` exists, exports `useCreateEntry()`, uses mutationFn calling POST /api/analyze, onMutate inserts temp entry with `crypto.randomUUID()` in id
    - `src/hooks/useDeleteEntry.ts` exists, exports `useDeleteEntry()`, uses mutationFn calling DELETE /api/entries/${entryId}
    - All three hooks call `queryClient.invalidateQueries({ queryKey: ["entries", "today"] })` in onSettled
    - All three hooks implement error rollback via `queryClient.setQueryData` with previous snapshot
    - All three hook test files exist and pass
    - `npm run test` passes (no regressions)
  </acceptance_criteria>
  <done>All three TanStack Query hooks created with optimistic update patterns, tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (GET /api/entries) | Untrusted `day` param crosses here — must validate regex |
| client→API (POST /api/analyze) | Content string crosses here — existing validation applies |
| client→API (DELETE /api/entries/[id]) | Entry ID crosses here — RLS + userId check on server |
| cache→UI | Optimistic temp entries in TanStack Query cache — never sent to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | GET /api/entries day param | mitigate | Regex validation: `/^\d{4}-\d{2}-\d{2}$/` at route entry |
| T-11-02 | Tampering | Optimistic cache entry | accept | Temp entries use `temp-` prefix IDs; never sent to server; onSettled refetch replaces with server truth |
| T-11-03 | Elevation of Privilege | Undo flow | mitigate | Query invalidation after delete ensures server truth wins; userId from session prevents cross-user deletion |
| T-11-SC | Tampering | npm installs | mitigate | No new packages installed — all deps already in package.json |
</threat_model>

<verification>
1. `npm run test` passes (all existing + new hook + utils tests)
2. `npm run build` passes (no type errors from new hooks/types)
3. JournalEntry interface in src/lib/types.ts matches MoodCalendar.tsx original (field-for-field)
4. GET /api/entries with ?day=YYYY-MM-DD returns correct day's entries
5. GET /api/entries with ?month=YYYY-MM still works (backward compatible)
6. Optimistic create inserts temp entry with crypto.randomUUID() id (not Date.now())
7. All three hooks import from @/lib/types (single source of truth)
</verification>

<success_criteria>
- 6 new files created (types.ts, utils.ts, 3 hooks)
- 4 new test files passing
- 1 API route extended with day param
- All 52+ existing tests still pass
- npm run build succeeds
</success_criteria>

<output>
Create `.planning/phases/11-dashboard-entry-list-optimistic-updates/11-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File |
|--------|------|------|
| `JournalEntry` | interface | src/lib/types.ts |
| `relativeTime` | function | src/lib/utils.ts |
| `useTodayEntries` | hook function | src/hooks/useTodayEntries.ts |
| `useCreateEntry` | hook function | src/hooks/useCreateEntry.ts |
| `useDeleteEntry` | hook function | src/hooks/useDeleteEntry.ts |
